# 台灣鳥類辨識器 (Taiwan Bird Classifier)

使用遷移式學習（Transfer Learning）技術識別台灣常見鳥類的智慧型分類系統。

## 🎯 專案特色

- 🦜 **鳥類辨識**：識別台灣常見三種八哥科鳥類（白尾八哥、家八哥、林八哥）
- 🤖 **遷移式學習**：使用 MobileNetV2 預訓練模型
- 🌐 **Streamlit 網頁介面**：簡潔直觀的上傳辨識介面
- 📊 **視覺化結果**：顯示預測機率和信心度
- 🚀 **可部署至 GitHub + Streamlit Cloud**

## 🚀 快速開始

### 1. 安裝依賴

```bash
# 建立虛擬環境
python3 -m venv venv
source venv/bin/activate  # macOS/Linux
# venv\Scripts\activate  # Windows

# 安裝套件
pip install -r requirements.txt
```

### 2. 準備訓練資料

執行資料下載腳本，自動從網路下載並整理訓練資料：

```bash
python src/prepare_data.py
```

資料結構：
```
data/
├── train/
│   ├── 白尾八哥/
│   ├── 家八哥/
│   └── 林八哥/
└── test/
    ├── 白尾八哥/
    ├── 家八哥/
    └── 林八哥/
```

### 3. 訓練模型

可以使用 Jupyter Notebook 訓練或直接執行訓練腳本：

**選項 A：使用 Notebook（推薦）**
```bash
jupyter notebook notebooks/bird_classifier_training.ipynb
```

**選項 B：使用腳本**
```bash
python src/train.py
```

訓練完成後會在 `models/` 資料夾生成模型檔案。

### 4. 啟動網頁應用

```bash
cd src
streamlit run app.py
```

或從專案根目錄：
```bash
streamlit run src/app.py
```

瀏覽器會自動開啟 `http://localhost:8501`

## 📂 專案結構

```
HW4/
├── data/                      # 訓練資料
│   ├── train/                 # 訓練集
│   └── test/                  # 測試集
├── models/                    # 訓練好的模型
│   └── bird_classifier.keras  # 模型檔案
├── src/                       # 原始碼
│   ├── app.py                # Streamlit 網頁應用
│   ├── train.py              # 訓練腳本
│   └── prepare_data.py       # 資料準備腳本
├── notebooks/                 # Jupyter Notebooks
│   └── bird_classifier_training.ipynb
├── requirements.txt           # 依賴套件
├── README.md                 # 專案說明
└── .gitignore               # Git 忽略檔案
```

## 🌐 部署到 Streamlit Cloud

### 步驟 1: 上傳到 GitHub

```bash
# 初始化 git（如果尚未初始化）
git init

# 添加檔案
git add .
git commit -m "Initial commit: Bird Classifier"

# 連接到 GitHub repository
git remote add origin https://github.com/你的用戶名/你的repo名稱.git
git branch -M main
git push -u origin main
```

### 步驟 2: 部署到 Streamlit Cloud

1. 前往 [Streamlit Cloud](https://streamlit.io/cloud)
2. 使用 GitHub 帳號登入
3. 點擊 "New app"
4. 選擇你的 repository
5. 設定：
   - **Main file path**: `src/app.py`
   - **Python version**: 3.9 或 3.10
6. 點擊 "Deploy!"

### 注意事項

- 確保 `models/bird_classifier.keras` 已上傳到 GitHub（或在 app.py 中加入下載邏輯）
- Streamlit Cloud 有記憶體限制，模型檔案不宜過大
- 首次部署可能需要等待幾分鐘

## 🎓 技術說明

### 遷移式學習 (Transfer Learning)

本專案使用 MobileNetV2 作為基礎模型：
- **預訓練權重**：ImageNet 資料集
- **凍結層**：保留預訓練的特徵提取層
- **自訂層**：添加適合鳥類分類的全連接層
- **訓練策略**：只訓練頂層，快速達到良好效果

### 模型架構

```
MobileNetV2 (凍結)
    ↓
GlobalAveragePooling2D
    ↓
Dense (256, relu)
    ↓
Dropout (0.5)
    ↓
Dense (3, softmax)
```

### 資料擴增 (Data Augmentation)

訓練時使用以下資料擴增技術提升模型泛化能力：
- 隨機旋轉
- 水平翻轉
- 縮放
- 平移
- 亮度調整

## 📊 效能指標

- **訓練準確率**: ~90%+
- **驗證準確率**: ~85%+
- **推論速度**: < 1秒/張

## 🐛 常見問題

### Q: 如何更換辨識的鳥類種類？

A: 修改 `data/train/` 資料夾結構，每個子資料夾代表一個類別，然後重新訓練模型。

### Q: 模型檔案太大無法上傳到 GitHub 怎麼辦？

A: 有兩種解決方案：
1. 使用 Git LFS (Large File Storage)
2. 在 `app.py` 中加入從雲端下載模型的邏輯

### Q: Streamlit Cloud 部署失敗？

A: 檢查：
- `requirements.txt` 是否正確
- Python 版本是否相容（建議 3.9-3.10）
- 檔案路徑是否正確（使用相對路徑）

## 📝 授權

本專案僅供教學使用。

## 🙏 致謝

- 參考炎龍老師的 [AI-Demo](https://github.com/yenlung/AI-Demo) 專案
- 使用 TensorFlow/Keras 和 MobileNetV2 預訓練模型
